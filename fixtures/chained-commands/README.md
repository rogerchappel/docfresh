# Chained commands fixture

Install with `npm install` and run the CLI with `npx docfresh`.

The later command intentionally names a missing package script.

```sh
npm run build && npm run missing
```
