# CommonMark fences fixture

Install with `npm install` and run the documented checks below.

~~~sh
npm run missing-npm
~~~

  ```sh
pnpm run missing-pnpm
  ```

   ~~~~sh
yarn missing-yarn
   ~~~~~

 ```sh
bun missing-bun
 ```

   ~~~sh docfresh: smoke
node -e "process.exit(7)"
   ~~~
