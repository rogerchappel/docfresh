# Guide

Repository-root references such as `src/example.js` resolve from nested docs.
Repository identifiers such as `rogerchappel/docfresh` are not file paths.
The explicitly relative `./missing.md` and root-based `src/absent.js` are
genuinely absent.
